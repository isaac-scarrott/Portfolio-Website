const size = {
  mobile: '768px',
  tablet: '1024px',
  computer: '2560px',
};

const device = {
  mobile: `(max-width: ${size.mobile})`,
  tablet: `(max-width: ${size.tablet})`,
  computer: `(max-width: ${size.computer})`,
};

export default device;